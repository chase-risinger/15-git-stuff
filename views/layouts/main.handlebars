<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Git Stuff!</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/">Git Stuff!</a>
                <div class="git-logo">
                
                </div>
            </h1>
            <nav>
                {{#if loggedIn}}
                <a href="/dashboard">Your listings</a>
                <button id="logout">logout</button>
                {{else}}
                <a href="/login" class= "login-here">Login Here</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}

        </main>
        <footer>
            Thanks for visiting!
        </footer>
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>