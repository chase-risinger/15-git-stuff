<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Git Stuff!</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/">Git Stuff!</a>
            </h1>
            <nav>
                {{#if loggedIn}}
                <a href="/dashboard">Your listings</a>
                <button id="logout">logout</button>
                {{else}}
                <a href="/login">login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            <h2>Are you looking to Git rid of stuff?</h2>
            <h3>But Facebook marketplace can't help?</h3>
            <h4>We are here to help!</h4>
            <h1>Welcome to Git Stuff!</h1>
            {{{ body }}}

        </main>
        <footer>
            Thanks for visiting!
        </footer>
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>